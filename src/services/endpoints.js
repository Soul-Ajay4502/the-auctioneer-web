const endpoints = Object.freeze({
    authentication: {
        login: '/auth/login',
        token: '/auth/token ',
    },
    league: {
        list: '/league'
    }

});

export default endpoints;
