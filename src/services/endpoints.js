const endpoints = Object.freeze({
    authentication: {
        login: "/auth/login",
        token: "/auth/token ",
    },
    league: {
        list: "/league",
        add: "/league/add",
        update: "/league/update",
    },
});

export default endpoints;
